#!/bin/bash

SCRIPT_DIR=~/.config/local/mitmproxy/

main() {
  local cwd=$(pwd)
  local options="-s $SCRIPT_DIR/cocproxy.py"

  cd "${cwd}"
  . $SCRIPT_DIR/functions.sh "$@"
  exec_mitmproxy "$@"

}

main "$@"
