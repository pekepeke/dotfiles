#!/bin/bash

if !which docker >/dev/null 2>&1; then
  echo "command not found : docker " >&2
fi
ID=$(docker images | peco | awk '{print $3}')
# echo $ID

if [ -n "$ID" ]; then
  for id in $ID ; do
    echo docker rmi $id
    sudo docker rmi $id
  done
fi



