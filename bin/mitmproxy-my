#!/bin/bash

SCRIPT_DIR=~/.config/local/mitmproxy/

main() {
  local cwd=$(pwd)
  local scriptdir=$(cd "$(dirname $0)"; pwd)
  local options=""

  cd "${cwd}"
  . $SCRIPT_DIR/functions.sh "$@"
  exec_mitmproxy "$@"

}

main "$@"
