package {
  import flash.display.*;
  import flash.text.*;

  public class <+FILENAME_NOEXT+> extends Sprite {
    public function <+FILENAME_NOEXT+>() {
      var textField:TextField = new TextField();
      textField.text = "Hello";
      addChild(textField);
    }
  }
}
